const factor = require('./recursion');


/*
test('Create an array with fibonacchi numbers', () => {
    expect(factor(12)).toEqual(479001600)
})


test('The greatest common divisor (gcd) of two positive numbers', () => {
    expect(factor(48,18)).toEqual(6)
})

test('Range of numbers', () => {
    expect(factor(1,9)).toEqual([2,3,4,5,6,7,8])
})

*/



test('Exponenta', () => {
    expect(factor(12, 2)).toEqual(144)
})